export default function FeedbackTab() {
  return (
    <button
      className="button-focus fixed right-0 top-1/2 z-40 -translate-y-1/2 rounded-l-md bg-purple px-2 py-4 text-xs font-semibold uppercase tracking-wide text-gallery transition-opacity hover:opacity-90"
      style={{ writingMode: "vertical-rl" }}
    >
      Feedback
    </button>
  );
}
